"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const decoder_1 = require("../RESP/decoder");
function transformMap(reply, typeMapping, transformValue) {
    if (!Array.isArray(reply)) {
        return reply;
    }
    const entries = [];
    for (let i = 0; i < reply.length; i += 2) {
        const key = reply[i].toString();
        entries.push([key, transformValue(key, reply[i + 1])]);
    }
    switch (typeMapping?.[decoder_1.RESP_TYPES.MAP]) {
        case Array:
            return entries.flat();
        case Map:
            return new Map(entries);
        default: {
            const object = {};
            for (const [key, value] of entries) {
                object[key] = value;
            }
            return object;
        }
    }
}
function transformArgument(reply, typeMapping) {
    return transformMap(reply, typeMapping, (key, value) => {
        if (key === 'arguments' && Array.isArray(value)) {
            return value.map(argument => transformArgument(argument, typeMapping));
        }
        return value;
    });
}
function transformDoc(reply, typeMapping) {
    return transformMap(reply, typeMapping, (key, value) => {
        switch (key) {
            case 'arguments':
                return Array.isArray(value)
                    ? value.map(argument => transformArgument(argument, typeMapping))
                    : value;
            case 'subcommands':
                return transformDocsMap(value, typeMapping);
            case 'reply_schema':
                return value;
            default:
                return value;
        }
    });
}
function transformDocsMap(reply, typeMapping) {
    return transformMap(reply, typeMapping, (_key, value) => transformDoc(value, typeMapping));
}
exports.default = {
    IS_READ_ONLY: true,
    /**
     * @see https://redis.io/commands/command-docs/
     */
    parseCommand(parser, commands) {
        parser.push('COMMAND', 'DOCS');
        if (commands !== undefined) {
            parser.pushVariadic(commands);
        }
    },
    transformReply: {
        2: (reply, _preserve, typeMapping) => transformDocsMap(reply, typeMapping),
        3: undefined
    }
};
//# sourceMappingURL=COMMAND_DOCS.js.map