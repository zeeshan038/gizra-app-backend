import { CommandParser } from '../client/parser';
import { NumberReply } from '../RESP/types';
import { RedisVariadicArgument } from './generic-transformers';
/**
 * Options for the SDIFFCARD command
 *
 * @property LIMIT - Cap the returned cardinality at this value; `LIMIT 0` means no limit
 */
export interface SDiffCardOptions {
    LIMIT?: number;
}
declare const _default: {
    readonly IS_READ_ONLY: true;
    readonly parseCommand: (this: void, parser: CommandParser, keys: RedisVariadicArgument, options?: SDiffCardOptions) => void;
    readonly transformReply: () => NumberReply;
};
export default _default;
//# sourceMappingURL=SDIFFCARD.d.ts.map