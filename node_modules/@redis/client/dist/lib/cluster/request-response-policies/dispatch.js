"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RESPONSE_REDUCERS = exports.REQUEST_ROUTERS = exports.remapAggregateReply = exports.NUMERIC_AGG_POLICIES = exports.reduceDefaultKeyed = exports.reduceDefaultKeyless = exports.reduceSpecial = exports.SPECIAL_RESPONSE_REDUCERS = exports.reduceRandomKey = exports.reduceSum = exports.reduceMax = exports.reduceMin = exports.reduceLogicalOr = exports.reduceLogicalAnd = exports.reduceAllSucceeded = exports.reduceOneSucceeded = exports.routeSpecial = exports.SPECIAL_REQUEST_ROUTERS = exports.routeDefaultKeyed = exports.routeDefaultKeyless = exports.routeMultiShard = exports.routeAllShards = exports.routeAllNodes = void 0;
const parser_1 = require("../../client/parser");
const decoder_1 = require("../../RESP/decoder");
const multi_shard_splitter_1 = require("./multi-shard-splitter");
const generic_aggregators_1 = require("./generic-aggregators");
const policies_constants_1 = require("../../command-metadata/policies-constants");
const ft_cursor_1 = require("./ft-cursor");
const scan_cursor_1 = require("./scan-cursor");
// --- request routers ---
const routeAllNodes = async (slots) => slots.getAllNodes().map(node => ({ getClient: () => slots.nodeClient(node) }));
exports.routeAllNodes = routeAllNodes;
const routeAllShards = async (slots) => slots.getAllMasterNodes().map(node => ({ getClient: () => slots.nodeClient(node) }));
exports.routeAllShards = routeAllShards;
/**
 * Splits the command into one sub-command per hash slot (using the COMMAND key
 * specs as the reconstruction recipe) and returns a plan entry per slot. Each
 * entry carries its own sub-parser, so core `_execute` routes it by that
 * slot's `firstKey` and handles MOVED/ASK with the sub-command's own key.
 */
const routeMultiShard = async (_slots, parser, _isReadonly, keySpecs) => {
    const subCommands = (0, multi_shard_splitter_1.splitMultiShardCommand)(parser.redisArgs, keySpecs);
    return Array.from(subCommands.values(), sub => ({
        parser: buildSubParser(sub),
        groupIndices: sub.groupIndices
    }));
};
exports.routeMultiShard = routeMultiShard;
/**
 * Rebuilds a `CommandParser` from a split sub-command, marking the keys at
 * their known positions so `firstKey` resolves to this slot's first key.
 */
function buildSubParser(sub) {
    const parser = new parser_1.BasicCommandParser();
    const keyPositions = new Set(sub.keyPositions);
    for (let i = 0; i < sub.args.length; i++) {
        const arg = sub.args[i];
        if (keyPositions.has(i)) {
            parser.pushKey(arg);
        }
        else {
            parser.push(arg);
        }
    }
    return parser;
}
// `nodeClient` connects lazily — with `minimizeConnections` a node may have
// no client until first use, and `.client!` would put `undefined` in the plan
// (breaking the post-reply hooks that attribute the reply to `plan[0].client`).
const routeDefaultKeyless = async (slots) => [{ client: await slots.nodeClient(slots.getRandomNode()) }];
exports.routeDefaultKeyless = routeDefaultKeyless;
const routeDefaultKeyed = async (slots, parser, isReadonly) => [{ client: (await slots.getClientAndSlotNumber(parser.firstKey, isReadonly)).client }];
exports.routeDefaultKeyed = routeDefaultKeyed;
/**
 * Uppercased command key ("RANDOMKEY", "MEMORY STATS") used both to look up a
 * per-command special handler and to label warnings. `commandIdentifier`
 * preserves the caller's casing, so normalize before matching.
 */
function specialKey(parser) {
    const { subcommand } = parser.commandIdentifier;
    const c = (0, ft_cursor_1.upperCommand)(parser);
    return subcommand ? `${c} ${subcommand.toUpperCase()}` : c;
}
/**
 * Special-request routers. Looked up by `COMMAND SUBCOMMAND` first, then bare
 * `COMMAND` — SCAN registers bare because its second argument is a cursor,
 * which the naive `commandIdentifier` mistakes for a subcommand.
 */
exports.SPECIAL_REQUEST_ROUTERS = {
    'FT.CURSOR READ': ft_cursor_1.routeFtCursor,
    'FT.CURSOR DEL': ft_cursor_1.routeFtCursor,
    SCAN: scan_cursor_1.routeScan
};
/**
 * Router for the `special` request policy. Commands with a dedicated handler
 * (e.g. FT.CURSOR sticky routing, cluster-wide SCAN) short-circuit into
 * `SPECIAL_REQUEST_ROUTERS` first. Everything else has non-trivial routing no
 * generic rule captures and no handler yet: route to a single (random) node
 * like a keyless command so it still works, but warn — the reply reflects
 * only that one node.
 */
const routeSpecial = async (slots, parser, isReadonly, keySpecs) => {
    const handler = exports.SPECIAL_REQUEST_ROUTERS[specialKey(parser)]
        ?? exports.SPECIAL_REQUEST_ROUTERS[parser.commandIdentifier.command.toUpperCase()];
    if (handler)
        return handler(slots, parser, isReadonly, keySpecs);
    console.warn(`node-redis: no cluster routing implemented for the "special" request policy of ` +
        `"${specialKey(parser)}"; routing to a single node. The reply may be incomplete.`);
    return [{ client: await slots.nodeClient(slots.getRandomNode()) }];
};
exports.routeSpecial = routeSpecial;
// --- response reducers ---
/**
 * `one_succeeded`: first fulfilled reply wins. When every node rejects the
 * policy requires surfacing one of the shard errors (e.g. SCRIPT KILL's
 * NOTBUSY), not the opaque `AggregateError` that `Promise.any` throws.
 */
const reduceOneSucceeded = async (promises) => {
    try {
        return await Promise.any(promises);
    }
    catch (err) {
        if (err instanceof AggregateError && err.errors.length > 0)
            throw err.errors[0];
        throw err;
    }
};
exports.reduceOneSucceeded = reduceOneSucceeded;
const reduceAllSucceeded = async (promises) => {
    const responses = await Promise.all(promises);
    return responses[0];
};
exports.reduceAllSucceeded = reduceAllSucceeded;
/** Lifts a synchronous all-replies aggregator into a response reducer. */
const reduceWith = (aggregate) => async (promises) => aggregate(await Promise.all(promises));
exports.reduceLogicalAnd = reduceWith(generic_aggregators_1.aggregateLogicalAnd);
exports.reduceLogicalOr = reduceWith(generic_aggregators_1.aggregateLogicalOr);
exports.reduceMin = reduceWith(generic_aggregators_1.aggregateMin);
exports.reduceMax = reduceWith(generic_aggregators_1.aggregateMax);
exports.reduceSum = reduceWith(generic_aggregators_1.aggregateSum);
/**
 * RANDOMKEY under `all_shards`: each master returns a random key from its own
 * keyspace (or nil when empty). Return one of the non-nil replies at random so
 * the result is a valid random key across the whole cluster and never a
 * false-nil when some shard is empty but others hold keys. All shards empty →
 * nil.
 */
const reduceRandomKey = async (promises) => {
    const responses = await Promise.all(promises);
    const keys = responses.filter(reply => reply != null);
    if (keys.length === 0)
        return responses[0];
    return keys[Math.floor(Math.random() * keys.length)];
};
exports.reduceRandomKey = reduceRandomKey;
/**
 * Per-command reducers for the `special` response policy, keyed like
 * `SPECIAL_REQUEST_ROUTERS` (`COMMAND SUBCOMMAND`, bare-command fallback). A
 * `special` response needs command-specific merging that no generic rule
 * captures; commands absent here hit `reduceSpecial`'s generic fallback. SCAN
 * (response also tipped `special`) needs no entry: its plan is single-node, so
 * the fallback passes the sole reply through and the cursor rewrite happens in
 * `finalizeScanCursor`.
 */
exports.SPECIAL_RESPONSE_REDUCERS = {
    RANDOMKEY: exports.reduceRandomKey
};
/**
 * Entry point for the `special` response policy: dispatch to a per-command
 * reducer if one exists, else fall back to the default-keyless reduction (sole
 * reply as is, or a merge of a fan-out) so the command works instead of
 * throwing. Warn on the fallback because the merged shape is unlikely to be
 * what the command really wants.
 */
const reduceSpecial = async (promises, parser) => {
    const reducer = exports.SPECIAL_RESPONSE_REDUCERS[specialKey(parser)]
        ?? exports.SPECIAL_RESPONSE_REDUCERS[parser.commandIdentifier.command.toUpperCase()];
    if (reducer)
        return reducer(promises, parser);
    if (promises.length > 1) {
        console.warn(`node-redis: no cluster aggregation implemented for the "special" response policy of ` +
            `"${specialKey(parser)}"; merging replies from ${promises.length} nodes. The result shape may be wrong.`);
    }
    return (0, exports.reduceDefaultKeyless)(promises);
};
exports.reduceSpecial = reduceSpecial;
const reduceDefaultKeyless = async (promises) => {
    const responses = await Promise.all(promises);
    // Merging is only meaningful for fan-out replies (e.g. KEYS under
    // all_shards); the single-target case must pass scalar replies through.
    if (responses.length === 1)
        return responses[0];
    return (0, generic_aggregators_1.aggregateMerge)(responses);
};
exports.reduceDefaultKeyless = reduceDefaultKeyless;
const reduceDefaultKeyed = async (promises, _parser, positionHints) => {
    const responses = await Promise.all(promises);
    // Unsplit (single-target read, or a multi_shard command that landed on one
    // slot): pass the sole reply through unchanged.
    if (!positionHints?.some(hint => hint !== undefined)) {
        return responses[0];
    }
    // Split multi_shard (e.g. MGET across slots): each sub-reply is an array in
    // its own group order; scatter element i back to its original group ordinal
    // so the result matches the caller's key order regardless of slot/arrival.
    const result = [];
    responses.forEach((reply, p) => {
        const indices = positionHints[p];
        if (!indices) {
            throw new Error('default-keyed reducer: split reply missing position hints');
        }
        const elements = reply;
        indices.forEach((groupIndex, i) => {
            result[groupIndex] = elements[i];
        });
    });
    return result;
};
exports.reduceDefaultKeyed = reduceDefaultKeyed;
/**
 * Response policies whose reducers compute over raw numbers (scalars or
 * number arrays). Per-node replies for these plans are decoded *without* the
 * caller's type mapping — a `NUMBER: String` mapping would otherwise feed
 * strings into the numeric aggregators and throw — and the caller's mapping
 * is applied to the aggregated result instead (`remapAggregateReply`).
 */
exports.NUMERIC_AGG_POLICIES = new Set([
    policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_SUM,
    policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_MIN,
    policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_MAX,
    policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_LOGICAL_AND,
    policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_LOGICAL_OR
]);
/**
 * Applies the caller's NUMBER type mapping to a numeric aggregate (scalar, or
 * an array for the element-wise reducers like SCRIPT EXISTS), so aggregated
 * fan-out replies keep the same shape a standalone client would return.
 * Aggregation itself runs in JS number space, so — unlike standalone decode —
 * a `NUMBER: String` mapping does not preserve integer precision above 2^53
 * (see cluster-policy-caveats.md).
 */
function remapAggregateReply(reply, typeMapping) {
    const map = typeMapping?.[decoder_1.RESP_TYPES.NUMBER];
    if (!map || map === Number)
        return reply;
    // NUMBER maps to NumberConstructor | StringConstructor; both are callable.
    const apply = map;
    return (Array.isArray(reply)
        ? reply.map(value => apply(value))
        : apply(reply));
}
exports.remapAggregateReply = remapAggregateReply;
// --- registries ---
exports.REQUEST_ROUTERS = {
    [policies_constants_1.REQUEST_POLICIES_WITH_DEFAULTS.ALL_NODES]: exports.routeAllNodes,
    [policies_constants_1.REQUEST_POLICIES_WITH_DEFAULTS.ALL_SHARDS]: exports.routeAllShards,
    [policies_constants_1.REQUEST_POLICIES_WITH_DEFAULTS.MULTI_SHARD]: exports.routeMultiShard,
    [policies_constants_1.REQUEST_POLICIES_WITH_DEFAULTS.SPECIAL]: exports.routeSpecial,
    [policies_constants_1.REQUEST_POLICIES_WITH_DEFAULTS.DEFAULT_KEYLESS]: exports.routeDefaultKeyless,
    [policies_constants_1.REQUEST_POLICIES_WITH_DEFAULTS.DEFAULT_KEYED]: exports.routeDefaultKeyed
};
exports.RESPONSE_REDUCERS = {
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.ONE_SUCCEEDED]: exports.reduceOneSucceeded,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.ALL_SUCCEEDED]: exports.reduceAllSucceeded,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_LOGICAL_AND]: exports.reduceLogicalAnd,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_LOGICAL_OR]: exports.reduceLogicalOr,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_MIN]: exports.reduceMin,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_MAX]: exports.reduceMax,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.AGG_SUM]: exports.reduceSum,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.SPECIAL]: exports.reduceSpecial,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.DEFAULT_KEYLESS]: exports.reduceDefaultKeyless,
    [policies_constants_1.RESPONSE_POLICIES_WITH_DEFAULTS.DEFAULT_KEYED]: exports.reduceDefaultKeyed
};
//# sourceMappingURL=dispatch.js.map