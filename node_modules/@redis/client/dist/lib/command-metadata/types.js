"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseCommandName = void 0;
/**
 * Parses a `COMMAND`-style command name into its module/command parts:
 * `"ping"` → `std.ping`, `"ft.search"` → `ft.search`. More than one dot is
 * invalid in Redis → `undefined`. Callers own case normalization.
 */
function parseCommandName(fullCommandName) {
    const parts = fullCommandName.split('.');
    if (parts.length === 1)
        return { moduleName: 'std', commandName: fullCommandName };
    if (parts.length === 2)
        return { moduleName: parts[0], commandName: parts[1] };
    return undefined;
}
exports.parseCommandName = parseCommandName;
//# sourceMappingURL=types.js.map